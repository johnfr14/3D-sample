export const interfaces = {
  kek: ["function getSample(uint) external"],
  alpha: ["function getSample(uint) external"],
  fomo: ["function getSample(uint) external"],
  fud: ["function getSample(uint) external"]
}